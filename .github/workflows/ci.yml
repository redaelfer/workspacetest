name: CI

"on":
  push: {}
  pull_request: {}

jobs:
  validate:
    name: Validate repository
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Verify expected files
        run: |
          test -f guide.md
          test -f workspace.md
          find . -maxdepth 2 -type f | sort

      - name: Intentional CI failure for PR validation
        run: |
          echo "This workflow is expected to fail for CI validation."
          exit 1
